services:
  postgres:
    image: postgres:16-alpine
    container_name: armoriq-postgres
    environment:
      POSTGRES_DB: armoriq
      POSTGRES_USER: armoriq
      POSTGRES_PASSWORD: armoriq
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: armoriq-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
